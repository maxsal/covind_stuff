message("*beep boop* loading libraries...")
librarian::shelf(
  tidyverse,
  janitor,
  glue,
  ggtext,
  extrafont,
  here,
  httr,
  patchwork,
  EpiEstim,
  maxsal/covid19india
)
message("*beep boop* libraries loaded!!")